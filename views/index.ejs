<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="title">
        <h1>Random Posts</h1>
        <button type="button" class="btn btn-outline-success">
            <a href="http://localhost:8080/posts/new" class="click">
                Create new post
            </a>
        </button>
    </div>
    <br>
    <% for(post of posts){ %>
        <div class="post card">
            <h3 class="user card-title">@<%= post.username %></h3>
            <h4 class="content card-text cont"><%= post.content %></h4>
            <br>
            <div class="arrange">
                <button type="button" class="btn btn-outline-secondary">
                    <a href="http://localhost:8080/posts/<%= post.id %>" class="card-link">
                        See in detail
                    </a>
                </button>
                &nbsp;&nbsp;
                <button type="button" class="btn btn-outline-secondary">
                    <a href="http://localhost:8080/posts/<%= post.id %>/edit" class="card-link">
                        Edit
                    </a>
                </button>
                &nbsp;&nbsp;
                <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
                    <button class="btn btn-outline-danger">Delete Post</button>
                </form>
            </div>
            
        </div>
        <br>
    <% } %>

    <br><br>
</body>
</html>

 <!-- nodemon index.js -->